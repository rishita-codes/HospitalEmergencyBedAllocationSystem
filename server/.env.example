# PostgreSQL database configuration for backend
DB_HOST=localhost
DB_NAME=hospital_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_PORT=5432
PORT=3001

# Optional: use a full database URL instead of individual values
# DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/hospital_db
